# npm configuration for better reliability
# Retry failed requests
fetch-retries=5
fetch-retry-mintimeout=20000
fetch-retry-maxtimeout=120000

# Timeout settings
fetch-timeout=300000

# Use npm registry (with fallback to mirror if needed)
registry=https://registry.npmjs.org/

# Alternative: Use npm registry mirror if main registry fails
# Uncomment the line below if npm registry continues to have issues
# registry=https://registry.npmmirror.com/

