name: bookyo-autonomous-config
version: 1.0.0
schema: v1

models:
  - name: gpt-5.1
    provider: openai
    model: gpt-5.1
    apikey: ${secrets.OPENAI_API_KEY}

  - name: gpt-4.1
    provider: openai
    model: gpt-4.1
    apikey: ${secrets.OPENAI_API_KEY}

priority:
  - gpt-5.1
  - gpt-4.1
